apiVersion: v1

kind: Service

metadata:
  name: bms-service

spec:
  type: NodePort

  selector:
    app: bms

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007
